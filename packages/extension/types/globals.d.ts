// Ambient build-time constants injected by Vite `define` (see vite.config.ts).
// Kept in a script-scope .d.ts (no imports/exports) so they are visible globally.

// The API base URL. Defaults to production; overridable via INKMARK_EXT_API_URL at build time.
declare const __API_BASE__: string
